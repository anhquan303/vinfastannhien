/*
 * ActiveLink Messages
 *
 * This contains all the text for the ActiveLink component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ActiveLink';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ActiveLink component!',
  },
});
