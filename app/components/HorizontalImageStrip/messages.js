/*
 * HorizontalImageStrip Messages
 *
 * This contains all the text for the HorizontalImageStrip component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HorizontalImageStrip';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HorizontalImageStrip component!',
  },
});
