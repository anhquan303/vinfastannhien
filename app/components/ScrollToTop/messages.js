/*
 * ScrollToTop Messages
 *
 * This contains all the text for the ScrollToTop component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ScrollToTop';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ScrollToTop component!',
  },
});
