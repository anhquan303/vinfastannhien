/*
 * Showroom Messages
 *
 * This contains all the text for the Showroom container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Showroom';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Showroom container!',
  },
});
